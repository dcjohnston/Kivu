var {Grid, Row, Col} = ReactBootstrap;

UserDash = React.createClass({

  render() {
    return (
      <Grid fluid={true}>
        <Row>
        </Row>
      </Grid>
    );
  }
});

export default UserDash;
